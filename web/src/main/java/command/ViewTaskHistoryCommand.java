package command;

import controller.RequestHandler;


/**
 * Просмотр истории отдельно, не реализовано, выводится в деталях таска
 */
public class ViewTaskHistoryCommand implements ICommand {

    @Override
    public String execute(RequestHandler content) {
        // TODO Просмотр истории отдельно, не реализовано, выводится в деталях таска
        return null;
    }
}
